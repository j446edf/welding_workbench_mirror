

# GUI NEEDS TO CREATE THIS


## LOAD USER DYNAMIC INPUTS
INCLUDE(UNITE=90)

##
####### Create Thermal loading (BC)- Initial Reference state
##
TIMPO=DEFI_CONSTANTE(VALE=gui_t_inf);		# temperature at infinity in degrees Celsius (standard ambient temperature) 24C at pass 1, 50C all others
TIMPP=DEFI_CONSTANTE(VALE=gui_t_ipp);		# Interpass temperature in degrees Celsius (standard ambient temperature)
TIMPO_W=TIMPO;					        # temperature at infinity in degrees Celsius (standard ambient temperature)
TIMP_SW=TIMPO;					        # temperature of heatsink
HECHA=DEFI_CONSTANTE(VALE=gui_hecha,);		# coefficient of heat exchange [W/(mm^2*K)]
SIG=DEFI_CONSTANTE(VALE=gui_sig,);   # boltmann constant, [W/(mm^2*K^4)]
EPS=DEFI_CONSTANTE(VALE=gui_eps,);			# emissivity of the grey body [reference 1]
F_SYM=DEFI_CONSTANTE(VALE=gui_f_sym ,);          # Heat flux in body

